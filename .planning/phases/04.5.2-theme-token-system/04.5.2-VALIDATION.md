---
phase: 4.5.2
slug: theme-token-system
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-27
---

# Phase 4.5.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | node:test (built-in) |
| **Config file** | none — run directly |
| **Quick run command** | `node --test test/` |
| **Full suite command** | `node --test test/` |
| **Estimated runtime** | ~2 seconds |

---

## Sampling Rate

- **After every task commit:** Visual spot-check in browser (dev server at port 4242)
- **After every plan wave:** Full visual check — dark mode (default) and light mode (DevTools emulation)
- **Before `/gsd:verify-work`:** Both themes render without undefined `var()` references (no fallback gray)
- **Max feedback latency:** 5 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| TBD | 01 | 1 | DSGN-03 | smoke | `node --test test/theme-tokens.js` | ❌ W0 | ⬜ pending |
| TBD | 01 | 1 | DSGN-04 | smoke | `node --test test/theme-tokens.js` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `test/theme-tokens.js` — verify theme.css exists and contains required token names (`:root`, `--bg-page`, `--text-primary`, etc.) and `@media (prefers-color-scheme: light)` block
- No framework install needed — `node:test` is built-in

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Dark mode renders identically to current appearance | DSGN-03 | Visual regression — color correctness is perceptual | Open dev server at port 4242, compare visually to pre-change state |
| Light theme is readable and properly contrasted | DSGN-04 | Visual — contrast and readability are perceptual | In DevTools, emulate `prefers-color-scheme: light`, verify all text is readable on light backgrounds |
| Status colors consistent across both themes | DSGN-03, DSGN-04 | Visual — semantic color mapping | Check complete/active/pending items in both dark and light modes |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 5s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
