---
phase: 04.5.4-detail-page-layout
plan: 01
subsystem: ui
tags: [markdown-rendering, css-grid, frontmatter, detail-page, layout]

requires:
  - phase: 04.5.3-dashboard-tile-redesign
    provides: CSS theme tokens and minimap structure used by detail page

provides:
  - Frontmatter stripping in /render endpoint (server-side, all .md files)
  - Three-column CSS grid skeleton for detail page (#detail-body with #phase-navigator, #phase-file-list, #phase-content)
  - nav-col-toggle button for collapsing the navigator column
  - Graceful renderTimeline() no-op when #phase-timeline element is absent

affects:
  - 04.5.4-02 (Plan 02 will populate #phase-navigator with vertical phase tree)

tech-stack:
  added: []
  patterns:
    - "CSS grid with transition on grid-template-columns for collapsible columns"
    - "sourcePath stored on #detail-body dataset instead of removed timeline container"

key-files:
  created: []
  modified:
    - src/server.js
    - test/server.test.js
    - public/index.html

key-decisions:
  - "DASH-19: Strip frontmatter before renderMarkdown() using tolerant /^---\\r?\\n[\\s\\S]*?\\r?\\n---\\r?\\n?/ regex"
  - "Store sourcePath on #detail-body dataset; selectPhase() falls back to #detail-body if #phase-timeline absent"
  - "renderTimeline() returns early (no-op) when #phase-timeline element is absent — graceful degradation until Plan 02"
  - "nav-col-toggle button prepended to #phase-file-list so it anchors the file list column visually"

patterns-established:
  - "Detail page 3-column grid: 200px navigator | 180px file-list | flex content"
  - "nav-collapsed class on #detail-body collapses navigator to 0px via grid-template-columns transition"

requirements-completed: [DASH-16, DASH-19]

duration: 3min
completed: 2026-03-29
---

# Phase 4.5.4 Plan 01: Server-side Frontmatter Stripping and Three-Column Detail Page Layout Summary

**Frontmatter stripped from all /render responses via server-side regex, and detail page replaced with a 3-column CSS grid (#phase-navigator | #phase-file-list | #phase-content) ready for Plan 02's vertical navigator.**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-03-29T02:30:00Z
- **Completed:** 2026-03-29T02:32:52Z
- **Tasks:** 2 (Task 1 TDD: RED + GREEN commits; Task 2: HTML/CSS + JS changes)
- **Files modified:** 3

## Accomplishments

- Server-side frontmatter stripping in /render handler with 3 new DASH-19 tests (Unix LF, no-frontmatter regression, Windows CRLF)
- Replaced horizontal `#phase-timeline-container` + 2-column `#docs-panel` with 3-column `#detail-body` CSS grid
- Added `#phase-navigator` empty column (populated by Plan 02), `nav-col-toggle` button, collapse transition support
- Made `renderTimeline()` a no-op if `#phase-timeline` is absent; `selectPhase()` reads `sourcePath` from `#detail-body`

## Task Commits

1. **Task 1 RED: Failing DASH-19 tests** - `ed7c1d0` (test)
2. **Task 1 GREEN: Frontmatter strip implementation** - `6b7f528` (feat)
3. **Task 2: Three-column grid HTML/CSS/JS** - `6e3ffd0` (feat)

## Files Created/Modified

- `src/server.js` - Added one-line frontmatter strip before renderMarkdown() call
- `test/server.test.js` - Added 3 DASH-19 tests (total: 121 passing)
- `public/index.html` - Replaced timeline + docs-panel with 3-column detail-body grid; updated JS references

## Decisions Made

- Used single regex `content.replace(/^---\r?\n[\s\S]*?\r?\n---\r?\n?/, '')` per CONTEXT.md decision — handles both LF and CRLF
- `sourcePath` stored on `#detail-body` dataset (not removed `#phase-timeline`) so click handlers in Plan 02 can read it
- `renderTimeline()` guard (`if (!container) return`) preserves backward compatibility without breaking existing test suite

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `#detail-body` 3-column skeleton is in place; `#phase-navigator` is empty and ready for Plan 02's vertical phase tree
- `nav-col-toggle` button renders at top of file-list column; toggle logic to be wired in Plan 02
- All 121 tests pass; browse view and dashboard card view completely unaffected

## Self-Check: PASSED

- src/server.js: FOUND
- test/server.test.js: FOUND
- public/index.html: FOUND
- 04.5.4-01-SUMMARY.md: FOUND
- Commit ed7c1d0 (RED tests): FOUND
- Commit 6b7f528 (GREEN implementation): FOUND
- Commit 6e3ffd0 (three-column grid): FOUND

---
*Phase: 04.5.4-detail-page-layout*
*Completed: 2026-03-29*
