---
phase: 06-distribution
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - README.md
autonomous: false
requirements:
  - DIST-01
  - DIST-02

must_haves:
  truths:
    - "README.md exists with project context, quick start, CLI reference, and dashboard screenshot placeholder"
    - "npm pack --dry-run shows clean tarball ready for publish"
    - "npm publish succeeds and npm view gsd-browser returns package metadata"
  artifacts:
    - path: "README.md"
      provides: "Project documentation for npm and GitHub"
      min_lines: 80
  key_links:
    - from: "README.md"
      to: "package.json"
      via: "Version and install instructions reference the published package"
      pattern: "npx gsd-browser"
---

<objective>
Create the project README and publish to npm.

Purpose: The README is the first thing users see on both GitHub and npm. It establishes what gsd-browser is, how to use it, and its relationship to the GSD workflow. Publishing to npm makes `npx gsd-browser` work for anyone.

Output: README.md with full documentation; package published to npm as gsd-browser@0.9.0.
</objective>

<execution_context>
@/Users/mstone/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mstone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-distribution/06-CONTEXT.md
@.planning/phases/06-distribution/06-RESEARCH.md
@.planning/phases/06-distribution/06-01-SUMMARY.md

@package.json
@bin/gsd-browser.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project README</name>
  <files>README.md</files>
  <action>
Create a detailed README.md following GitHub best practices. Structure per user's locked decisions:

1. **Title and tagline**: "gsd-browser" — Browse markdown artifacts that AI agents write, fresh from disk
2. **What is this**: Brief context on GSD (get-shit-done) workflow, why this tool exists, relationship to get-shit-done-cc (Claude Code planning framework). This is a companion tool that lets you read the .planning/ artifacts that GSD agents produce while they're working.
3. **Screenshot placeholder**: `![Dashboard](docs/screenshot-dashboard.png)` — placeholder for a dashboard screenshot the user will add later. Include a comment noting "<!-- TODO: Add actual screenshot -->"
4. **Quick Start**:
   ```
   npx gsd-browser
   ```
   Explain: runs in CWD, auto-discovers .planning/, docs/, README.md. If found, auto-registers and opens browser.
5. **Installation** (optional permanent install):
   ```
   npm install -g gsd-browser
   ```
6. **Usage / CLI Reference**: Document all commands and flags:
   - `gsd-browser` (no args) — start server
   - `gsd-browser add [path]` — register source (default: CWD)
   - `gsd-browser add . --name my-project` — register with custom name
   - `gsd-browser remove <name|path>` — remove source
   - `gsd-browser list` — show registered sources
   - `--port <n>` / `-p` — custom port (default: 4242)
   - `--no-open` — don't open browser
   - `--version` / `-v` — print version
   - `--help` / `-h` — show help
7. **Features**: Bullet list of key features:
   - GFM rendering with syntax highlighting (Shiki) and Mermaid diagrams
   - Multi-project dashboard with phase timeline and progress tracking
   - File tree browser with source switching
   - Fresh-from-disk on every request (no caching, no stale reads)
   - Convention-based discovery (.planning/, docs/, README.md)
   - Branch-aware GSD progress across git branches
   - Dark theme with Catppuccin colors
   - Localhost-only with CSP headers (security-first)
8. **How it works**: Brief technical overview — Node.js + Fastify, markdown-it + Shiki + Mermaid, vanilla JS frontend, no build step
9. **Configuration**: Config file location (`~/.config/gsd-browser/sources.json` on Linux/macOS), mention `open` preference
10. **License**: MIT

Do NOT include emojis. Use clean, professional markdown. Keep it factual and useful.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const r = fs.readFileSync('README.md','utf8'); const lines = r.split('\n').length; console.log(lines + ' lines'); if (lines < 80) process.exit(1); if (!r.includes('npx gsd-browser')) process.exit(1); if (!r.includes('MIT')) process.exit(1); console.log('OK')"</automated>
  </verify>
  <done>README.md exists with 80+ lines covering quick start, CLI reference, features, and license. Contains `npx gsd-browser` install instructions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify distribution package and publish to npm</name>
  <files>README.md</files>
  <action>
Present the complete distribution package for user review and npm publication. User will verify README accuracy, test first-run behavior, review npm pack output, and publish.
  </action>
  <what-built>
Complete distribution package: README.md, package.json with version 0.9.0 and npm metadata, first-run auto-registration, auto-open browser, startup banner with version and conventions.
  </what-built>
  <how-to-verify>
1. Review README.md — does it accurately describe the project?
2. Run `npm pack --dry-run` — verify tarball contains only intended files (src/, bin/gsd-browser.cjs, public/, README.md, LICENSE, package.json)
3. Test first-run: remove config (`rm ~/.config/gsd-browser/sources.json` or equivalent), cd to this repo, run `node bin/gsd-browser.cjs` — should auto-register, show banner with "(auto-registered)", open browser
4. Test --no-open: `node bin/gsd-browser.cjs --no-open` — server starts, browser does not open
5. When satisfied, publish: `npm publish`
6. Verify: `npm view gsd-browser` — should show package metadata
  </how-to-verify>
  <verify>
    <automated>npm pack --dry-run 2>&1 | grep -c 'README.md' | xargs test 1 -eq</automated>
  </verify>
  <done>User has reviewed README, tested first-run UX, and published package to npm. npm view gsd-browser returns valid metadata.</done>
  <resume-signal>Type "approved" after reviewing README and testing the package, or describe issues. If you've published to npm, confirm with the npm view output.</resume-signal>
</task>

</tasks>

<verification>
1. README.md exists with comprehensive documentation
2. `npm pack --dry-run` shows clean tarball
3. After user publishes: `npm view gsd-browser` returns package metadata
4. `npx gsd-browser` works on a clean machine (user verifies)
</verification>

<success_criteria>
- README.md covers quick start, CLI reference, features, configuration, and license
- Package published to npm as gsd-browser@0.9.0
- `npm view gsd-browser` returns valid metadata
- `npx gsd-browser` works for zero-install usage
</success_criteria>

<output>
After completion, create `.planning/phases/06-distribution/06-02-SUMMARY.md`
</output>
