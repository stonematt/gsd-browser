---
phase: 04.5.1-dashboard-ux-polish
plan: 01
subsystem: api
tags: [server, parsing, frontmatter, requirements, tdd]

# Dependency graph
requires:
  - phase: 04.5-gsd-dashboard
    provides: parsePlanFrontmatter and buildPlanDetails functions used as extension point
provides:
  - parsePlanFrontmatter returns requirements: string[] alongside wave and dependsOn
  - buildPlanDetails propagates requirements per plan object
  - API /api/projects/:name/detail now includes requirements per plan in planDetails
affects:
  - 04.5.1-dashboard-ux-polish plans requiring requirement chip/badge rendering

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Multi-format YAML parsing: inline array and multi-line list both handled by ordered regex try — inline first, then multi-line

key-files:
  created: []
  modified:
    - src/server.js
    - test/server.test.js

key-decisions:
  - "Try inline requirements array format first, then multi-line list — inline is simpler to match and less common in actual PLAN.md files"
  - "Strip leading dashes and whitespace from multi-line list items via .map(line => line.replace(/^[ \\t]+-[ \\t]+/, '').trim()) — no external YAML parser needed"

patterns-established:
  - "YAML field extension pattern: add field to result object default, add regex branch, propagate through buildPlanDetails — same shape as wave/dependsOn"

requirements-completed: [DASH-12]

# Metrics
duration: 1min
completed: 2026-03-25
---

# Phase 4.5.1 Plan 01: Requirements Parsing Summary

**parsePlanFrontmatter now extracts requirements arrays (both inline and multi-line YAML) and buildPlanDetails propagates them into API responses**

## Performance

- **Duration:** 1 min
- **Started:** 2026-03-25T19:54:59Z
- **Completed:** 2026-03-25T19:56:21Z
- **Tasks:** 1 (TDD — 2 commits: test then feat)
- **Files modified:** 2

## Accomplishments
- Extended parsePlanFrontmatter to parse `requirements:` in both inline array (`[DASH-01, DASH-02]`) and multi-line YAML list formats
- buildPlanDetails now includes `requirements: string[]` in each returned plan detail object
- 7 new DASH-12 unit tests covering all edge cases (absent field, empty section, both formats, coexistence with wave/dependsOn, buildPlanDetails propagation, null-file fallback)
- All 137 tests pass with no regressions

## Task Commits

Each task was committed atomically:

1. **RED: failing tests for parsePlanFrontmatter requirements parsing** - `464a497` (test)
2. **GREEN: requirements parsing implementation** - `da2623e` (feat)

_Note: TDD tasks may have multiple commits (test then feat)_

## Files Created/Modified
- `src/server.js` - parsePlanFrontmatter extended with requirements parsing; buildPlanDetails propagates requirements
- `test/server.test.js` - 7 new DASH-12 tests; parsePlanFrontmatter and buildPlanDetails added to imports

## Decisions Made
- Inline array format tried first (same regex approach as depends_on), multi-line list as fallback — avoids ambiguity if both somehow present
- Multi-line regex `/((?:[ \t]+-[ \t]+\S[^\n]*\n?)+)/` requires at least one non-whitespace after dash — prevents matching empty list items

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Requirements data is now available in the API response via planDetails[].requirements
- DASH-12 requirement chips/badge rendering in the dashboard frontend can proceed in the next plan
- No blockers

---
*Phase: 04.5.1-dashboard-ux-polish*
*Completed: 2026-03-25*

## Self-Check: PASSED

- src/server.js: FOUND
- test/server.test.js: FOUND
- SUMMARY.md: FOUND
- Commit 464a497 (RED tests): FOUND
- Commit da2623e (GREEN implementation): FOUND
