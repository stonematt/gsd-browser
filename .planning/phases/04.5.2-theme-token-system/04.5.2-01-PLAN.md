---
phase: 04.5.2-theme-token-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/styles/theme.css
  - test/theme-tokens.js
  - public/sources.html
  - src/renderer.js
  - public/styles/markdown.css
autonomous: true
requirements:
  - DSGN-03
  - DSGN-04

must_haves:
  truths:
    - "theme.css defines all semantic color tokens in :root with Catppuccin Mocha values"
    - "theme.css contains @media (prefers-color-scheme: light) block with Catppuccin Latte overrides"
    - "sources.html uses var(--token) references instead of hardcoded hex colors"
    - "renderer.js buildPage breadcrumb uses var(--text-link) and var(--text-muted) instead of hardcoded hex"
    - "markdown.css upstream var() references replaced with semantic token names"
  artifacts:
    - path: "public/styles/theme.css"
      provides: "All color tokens for dark (Mocha) and light (Latte) themes"
      contains: ":root"
    - path: "test/theme-tokens.js"
      provides: "Smoke test verifying theme.css structure and token presence"
      contains: "describe"
  key_links:
    - from: "public/sources.html"
      to: "public/styles/theme.css"
      via: "link rel=stylesheet in head"
      pattern: "href.*styles/theme\\.css"
    - from: "src/renderer.js"
      to: "public/styles/theme.css"
      via: "link rel=stylesheet in buildPage template"
      pattern: "styles/theme\\.css"
---

<objective>
Create the theme token system (theme.css) with Catppuccin Mocha dark default and Latte light override, then migrate the smaller consumer files (sources.html, renderer.js, markdown.css) from hardcoded hex to token references.

Purpose: Establishes the single source of truth for all color tokens before the large index.html migration in Plan 02.
Output: theme.css with all tokens, 3 consumer files migrated, smoke test passing.
</objective>

<execution_context>
@/Users/mstone/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mstone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.5.2-theme-token-system/04.5.2-CONTEXT.md
@.planning/phases/04.5.2-theme-token-system/04.5.2-RESEARCH.md

@public/sources.html
@src/renderer.js
@public/styles/markdown.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create theme.css token file and smoke test</name>
  <files>public/styles/theme.css, test/theme-tokens.js</files>
  <action>
Create `public/styles/theme.css` with the complete token system. Use the exact Catppuccin official hex values from the RESEARCH.md color mapping table.

**`:root` block (Catppuccin Mocha — dark default):**

Backgrounds:
- `--bg-page: #1e1e2e` (Base)
- `--bg-surface: #181825` (Mantle)
- `--bg-overlay: #313244` (Surface 0)
- `--bg-code: #181825` (Mantle — same as surface, distinct semantic role)

Borders:
- `--border-default: #45475a` (Surface 1)
- `--border-muted: #313244` (Surface 0)

Text:
- `--text-primary: #cdd6f4` (Text)
- `--text-secondary: #bac2de` (Subtext 1)
- `--text-muted: #6c7086` (Overlay 0)
- `--text-dimmed: #585b70` (Surface 2)
- `--text-link: #89b4fa` (Blue)
- `--text-bright: #cdd6f4` (Text)

Status:
- `--status-complete: #a6e3a1` (Green)
- `--status-active: #89b4fa` (Blue)
- `--status-pending: #585b70` (Surface 2)
- `--status-complete-text: #a6e3a1`
- `--status-active-text: #89b4fa`
- `--status-pending-text: #6c7086` (Overlay 0)
- `--status-complete-bg: rgba(166, 227, 161, 0.15)`
- `--status-complete-border: rgba(166, 227, 161, 0.4)`
- `--status-danger: #f38ba8` (Red)
- `--status-danger-bg: rgba(243, 139, 168, 0.1)`
- `--status-danger-border: rgba(243, 139, 168, 0.3)`

Interactive:
- `--btn-primary-bg: #a6e3a1` (Green)
- `--btn-primary-text: #1e1e2e` (Base)
- `--btn-primary-hover-bg: #94e2d5` (Teal)
- `--btn-hover-bg: #45475a` (Surface 1)

Focus/Active States (semi-transparent — all based on Mocha Blue #89b4fa):
- `--focus-ring: rgba(137, 180, 250, 0.15)`
- `--hover-faint: rgba(137, 180, 250, 0.05)`
- `--hover-subtle: rgba(137, 180, 250, 0.08)`
- `--active-bg: rgba(137, 180, 250, 0.12)`
- `--active-ring: rgba(137, 180, 250, 0.3)`
- `--active-glow: rgba(137, 180, 250, 0.5)`

**`@media (prefers-color-scheme: light)` block (Catppuccin Latte):**
Override ALL token values with Latte equivalents. Use RESEARCH.md Latte column values. For semi-transparent tokens, use Latte Blue (#1e66f5) and Latte Green (#40a02b) and Latte Red (#d20f39) as the base colors with same alpha values.

**Smoke test** (`test/theme-tokens.js`):
Using `node:test` and `node:fs`, verify:
1. `public/styles/theme.css` exists
2. File contains `:root {` block
3. File contains `@media (prefers-color-scheme: light)` block
4. File contains all required token names: `--bg-page`, `--bg-surface`, `--text-primary`, `--text-link`, `--status-complete`, `--status-active`, `--status-pending`, `--btn-primary-bg`
5. File does NOT contain any bare hex values outside of var() definitions (i.e., tokens are properly defined, not consumed)
  </action>
  <verify>
    <automated>node --test test/theme-tokens.js</automated>
  </verify>
  <done>theme.css defines all semantic tokens for both Mocha and Latte palettes. Smoke test passes confirming file structure and token presence.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate sources.html, renderer.js, and markdown.css to token references</name>
  <files>public/sources.html, src/renderer.js, public/styles/markdown.css</files>
  <action>
**sources.html** (~20+ hex replacements):
1. Add `<link rel="stylesheet" href="/styles/theme.css">` in `<head>` BEFORE any inline `<style>` block.
2. Replace every hardcoded hex color value in the inline `<style>` block with the appropriate `var(--token-name)` reference. Use the mapping from RESEARCH.md:
   - `#0d1117` -> `var(--bg-page)`
   - `#161b22` -> `var(--bg-surface)`
   - `#21262d` -> `var(--border-default)`
   - `#30363d` -> `var(--border-muted)`
   - `#c9d1d9` -> `var(--text-primary)`
   - `#f0f6fc` -> `var(--text-bright)`
   - `#8b949e` -> `var(--text-muted)`
   - `#484f58` -> `var(--text-dimmed)`
   - `#58a6ff` -> `var(--text-link)`
   - `#238636` -> `var(--btn-primary-bg)`
   - `#2ea043` -> `var(--btn-primary-hover-bg)`
   - `#3fb950` -> `var(--status-complete)`
   - `#f85149` -> `var(--status-danger)`
   - All `rgba(...)` values -> corresponding semi-transparent tokens
3. Verify NO hardcoded hex color values remain in the inline `<style>` block (layout values like widths, margins in px are fine).

**renderer.js** (2 replacements in buildPage template):
1. Add `<link rel="stylesheet" href="/styles/theme.css">` in the `<head>` section of the `buildPage()` HTML template string, BEFORE the markdown.css link.
2. Replace `color:#58a6ff` with `color:var(--text-link)` in the home link.
3. Replace `color:#8b949e` with `color:var(--text-muted)` in the breadcrumb separator.

**markdown.css** (upstream var refs + hardcoded hex):
1. Replace the GitHub-style `var()` references with semantic token names:
   - `var(--borderColor-accent-emphasis)` -> `var(--text-link)`
   - `var(--borderColor-muted)` -> `var(--border-muted)`
   - `var(--fgColor-danger)` -> `var(--status-danger)`
   - `var(--bgColor-danger-muted)` -> `var(--status-danger-bg)`
2. Replace ALL hardcoded hex values in the file with appropriate `var(--token)` references using the mapping table. Pay attention to:
   - `#e6edf3` (body text) -> `var(--text-primary)`
   - `#151b23` (code bg) -> `var(--bg-code)`
   - `#9198a1` (muted) -> `var(--text-muted)`
   - `#f85149` (danger) -> `var(--status-danger)`
   - All other hex values per the RESEARCH.md inventory
3. Change `.markdown-body { color-scheme: dark; }` to `.markdown-body { color-scheme: dark light; }` per RESEARCH.md pitfall #3.
4. Leave `.pl-*` syntax highlight classes with their hardcoded hex values — these are Shiki fallback only and out of scope per RESEARCH.md.
  </action>
  <verify>
    <automated>node --test test/theme-tokens.js && node --test test/renderer.test.js && node --test test/server.test.js</automated>
  </verify>
  <done>sources.html, renderer.js, and markdown.css use var(--token) references for all colors. No hardcoded hex in sources.html inline styles (except .pl-* classes in markdown.css). All existing tests pass.</done>
</task>

</tasks>

<verification>
- `node --test test/` — all existing tests pass
- `grep -c '#[0-9a-fA-F]\{6\}' public/sources.html` returns 0 (no hex in inline styles)
- `grep -c 'color:#' src/renderer.js` returns 0
- `public/styles/theme.css` contains both `:root` and `@media (prefers-color-scheme: light)` blocks
</verification>

<success_criteria>
- theme.css exists with complete Mocha and Latte token definitions
- sources.html, renderer.js, and markdown.css migrated to token references
- All existing tests pass
- Smoke test for theme.css passes
</success_criteria>

<output>
After completion, create `.planning/phases/04.5.2-theme-token-system/04.5.2-01-SUMMARY.md`
</output>
